server:
  port: 8888
spring:
  application:
    name: config-service

---
spring:
  profiles: default
  cloud:
    config:
      server:
        git:
           uri: file://${CONFIG_REPO_PATH:/Users/kimm5/_dev/spring-cloud-event-sourceing-pcf-config}

---
spring:
  profiles: cloud
  cloud:
    config:
       server:
         git:
#          uri: https://github.com/kbastani/spring-cloud-event-sourcing-cf-config.git
           uri: https://github.com/myminseok/spring-cloud-event-sourceing-pcf-config
